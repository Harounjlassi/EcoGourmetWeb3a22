<form method="post" action="{{ path('app_events_delete', {'eventId': event.eventId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.eventId) }}">
    <button class="btn">Delete</button>
</form>
